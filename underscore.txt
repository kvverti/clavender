_ + 3  -->  def(a) => a + 3
3 + _ + 1  -->  (def(a) => 3 + a) + 1
(3 + _) + 1  -->  (def(a) => 3 + a) + 1

ls map str(_)  -->  ls map (def(a) => str(a))
ls fold ({}, _ ++ _)  -->  ls fold ({}, def(a, b) => a ++ b)

ls map str _  -->  ls map (def(a) => str(a))

_(1)  -->  def(a) => a(1)
_(1 + _)  -->  def(a) => a(def(b) => 1 + b)

{ _, 3, _ }  -->  def(a, b) => { a, 3, b }
{ _, 3, str(_) }  -->  def(a) => { a, 3, def(b) => str(b) }
